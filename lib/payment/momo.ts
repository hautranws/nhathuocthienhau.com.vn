export async function createMoMoUrl({ orderId, amount, orderInfo }: any) {
  // Code thật sẽ viết sau. Giờ trả về link giả để không bị lỗi.
  console.log("Đang gọi MoMo cho đơn:", orderId);
  return "https://momo.vn"; 
}